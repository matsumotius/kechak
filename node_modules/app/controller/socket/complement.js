var tiqavService = require('app/service/tiqav');
exports.tiqav = function(io, socket, query){
  var search = tiqavService.search(query.text);
  search.on('end', function(list){
    socket.emit('complement/tiqav', (list ? list : ''));
  });
};
