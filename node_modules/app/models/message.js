var Mongoose = require('mongoose');
var mongoose = new Mongoose.Mongoose();
var db       = mongoose.connect('mongodb://localhost:27017/kechak');

var Schema   = Mongoose.Schema;
var Message  = new Schema({
});
