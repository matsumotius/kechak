var jsonUtil = module.exports = {
  parse : function(body){
    if(typeof body == 'string'){
      try {
        return JSON.parse(body);
      } catch(e) {
        return null;
      }
    } else {
      return body;
    }
  }
};
